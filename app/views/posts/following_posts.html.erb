<h2>フォロー中のツイート</h2>
<% @users.each do |user| %>
  <% @posts = Post.where(user_id: user.id).limit(1).order("created_at DESC") %>
  <% @posts.each do |tweet| %>
    <div class="contents row" >
      <div class="name3">投稿者:<%= link_to user.user_name, user_path(user), method: :get %>&nbsp;&nbsp;<%= link_to user.user_name + 'のツイート一覧へ', user__path(user), {class: 'btn btn-primary withripple' }%></div>
      <div class="name2">投稿日時:<%= post.created_at.strftime("%Y-%m-%d %H:%M:%S") %></div>
      <div class="name2"><%= link_to '詳細', post_path(post), method: :get %></div>
      <div class="name3"><%= simple_format(post.content) %></div>
    </div>
  <% end %>
<% end %>
  <%= paginate(@users) %>
</div>